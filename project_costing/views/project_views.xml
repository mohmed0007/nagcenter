<?xml version="1.0" encoding="utf-8"?>
<odoo>
	<data>
            <record id="edit_project" model="ir.ui.view">
                <field name="name">project.project.form</field>
                <field name="model">project.project</field>
                 <field name="inherit_id" ref="project.edit_project" />
                <field name="arch" type="xml">
                    <field name="analytic_account_id" position="after">
                        <field name="create_location" />
                        <field name="project_warehouse_id" />
                        <field name="view_location_id" />
                        <field name="location_id" domain="[('project_id','=',active_id)]" 
                        context="{'default_project_id': active_id, 'default_view_location_id': view_location_id}" />
                    </field>
                    <xpath expr="//div[@name='button_box']" position="inside">
                        <button type="object" name="action_create_location"
							class="oe_stat_button" icon="fa-truck" string="Project Location" 
                            attrs="{'invisible':[('create_location','!=',True)]}">
							</button>
                    </xpath>
                </field>
            </record>

    </data>
</odoo>